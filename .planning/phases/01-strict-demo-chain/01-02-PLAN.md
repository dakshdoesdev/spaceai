---
phase: 01-strict-demo-chain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - satellite_edge_node/orbital_pass.py
  - satellite_edge_node/detectors.py
  - scripts/check_model_ready.py
  - tests/test_yolo_detector.py
  - tests/test_model_readiness_eval.py
requirements:
  - HON-01
  - HON-02
  - VAL-03
autonomous: true
must_haves:
  truths:
    - "User can run model readiness and clearly see whether strict YOLO is available."
    - "Strict YOLO fails loudly when weights or ultralytics are missing unless fallback is explicitly requested."
    - "Baseline and fallback outputs remain visibly simulated in payloads and telemetry metadata."
  artifacts:
    - path: "scripts/check_model_ready.py"
      provides: "Model readiness preflight"
      contains: "ready_for_strict_yolo"
    - path: "satellite_edge_node/orbital_pass.py"
      provides: "Strict detector CLI failure boundary"
      contains: "Detector setup failed"
    - path: "satellite_edge_node/detectors.py"
      provides: "Explicit baseline fallback metadata"
      contains: "fallback_used=True"
    - path: "tests/test_yolo_detector.py"
      provides: "Detector strict/fallback regression coverage"
      contains: "fallback"
    - path: "tests/test_model_readiness_eval.py"
      provides: "Readiness JSON regression coverage"
      contains: "ready_for_strict_yolo"
  key_links:
    - from: "scripts/check_model_ready.py"
      to: "models/brick_kiln_yolo.pt and ultralytics"
      via: "readiness JSON fields"
      pattern: "weights_exist.*ultralytics_available.*ready_for_strict_yolo"
    - from: "satellite_edge_node/orbital_pass.py"
      to: "satellite_edge_node.detectors.build_detector_with_fallback"
      via: "allow_baseline_fallback flag"
      pattern: "allow_baseline_fallback"
---

<objective>
Harden detector readiness, strict YOLO failure, and simulation/fallback metadata for Phase 1.

Purpose: HON-01, HON-02, and VAL-03 require the demo to be technically honest: baseline is simulation, strict YOLO is only real when setup exists, and fallback is opt-in.
Output: Readiness checks and tests that make detector status and fallback behavior unambiguous.
</objective>

<execution_context>
@/home/dux/Work/tries/SpaceAI/.codex/get-shit-done/workflows/execute-plan.md
@/home/dux/Work/tries/SpaceAI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-strict-demo-chain/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@satellite_edge_node/orbital_pass.py
@satellite_edge_node/detectors.py
@satellite_edge_node/yolo_detector.py
@satellite_edge_node/payloads.py
@scripts/check_model_ready.py
@tests/test_yolo_detector.py
@tests/test_model_readiness_eval.py

<interfaces>
From `scripts/check_model_ready.py`:
```python
def check_model_ready(model_path: Path = DEFAULT_MODEL_PATH) -> dict: ...
```
Expected keys: `model_path`, `weights_exist`, `ultralytics_available`, `ready_for_strict_yolo`, `status`, `missing`.

From `satellite_edge_node/detectors.py`:
```python
def build_detector_with_fallback(
    mode: str = "baseline",
    *,
    model_path: Path = DEFAULT_MODEL_PATH,
    confidence_threshold: float = 0.25,
    fallback_to_baseline: bool = False,
) -> tuple[Detector, str | None]: ...
```

From `satellite_edge_node/payloads.py`:
```python
def telemetry_record(...) -> dict[str, Any]: ...
def build_transmission_payload(...) -> dict[str, Any]: ...
```
Both must preserve `detector_mode`, `detector_is_real`, `simulated`, `fallback_used`, and fallback reason when present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI-level strict YOLO and readiness tests</name>
  <files>tests/test_yolo_detector.py, tests/test_model_readiness_eval.py</files>
  <read_first>
    - tests/test_yolo_detector.py
    - tests/test_model_readiness_eval.py
    - satellite_edge_node/orbital_pass.py
    - scripts/check_model_ready.py
  </read_first>
  <behavior>
    - `orbital_pass.main()` returns `2` when `--detector yolo` points at missing weights and fallback is not requested.
    - The strict-failure message contains "Detector setup failed" and tells the user how to opt into explicit baseline simulation/fallback.
    - `check_model_ready()` reports missing weights and missing ultralytics status in machine-readable JSON fields.
  </behavior>
  <action>
    Add tests before production edits where missing coverage exists. Use `tempfile.TemporaryDirectory()` and call `satellite_edge_node.orbital_pass.main()` with patched `sys.argv` only if the repo already uses that pattern; otherwise test the subprocess with `python -m satellite_edge_node.orbital_pass --raw-tiles <tmp_raw> --transmission-queue <tmp_queue> --detector yolo --model-path <missing.pt>`. Assert exit code `2` and the loud failure text. In readiness tests, assert `check_model_ready(missing_path)` includes `ready_for_strict_yolo=False`, `status="real detector unavailable"`, a `weights not found` item, and an `ultralytics_available` boolean.
  </action>
  <verify>
    <automated>python -m pytest tests/test_yolo_detector.py tests/test_model_readiness_eval.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if strict YOLO silently falls back without `--allow-baseline-fallback`.
    - Tests fail if readiness JSON lacks `ready_for_strict_yolo`, `weights_exist`, or `ultralytics_available`.
    - No test requires real `ultralytics` or `models/brick_kiln_yolo.pt`.
  </acceptance_criteria>
  <done>Strict detector and readiness behavior is locked by automated tests without requiring optional YOLO setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve explicit simulation and fallback metadata</name>
  <files>satellite_edge_node/detectors.py, satellite_edge_node/orbital_pass.py, scripts/check_model_ready.py, tests/test_yolo_detector.py, tests/test_model_readiness_eval.py</files>
  <read_first>
    - satellite_edge_node/detectors.py
    - satellite_edge_node/orbital_pass.py
    - satellite_edge_node/payloads.py
    - scripts/check_model_ready.py
    - tests/test_yolo_detector.py
    - tests/test_model_readiness_eval.py
    - .planning/phases/01-strict-demo-chain/01-RESEARCH.md
  </read_first>
  <behavior>
    - Baseline detections and payloads remain `detector_is_real=false`, `simulated=true`, `fallback_used=false`, `detector_mode=baseline`.
    - Explicit fallback detections and payloads are `detector_is_real=false`, `simulated=true`, `fallback_used=true`, `detector_mode=fallback`, and include `fallback_reason`.
    - Strict YOLO setup failures are not swallowed unless `allow_baseline_fallback=True`.
  </behavior>
  <action>
    Make only the narrow code edits required for the tests. If current code already satisfies the behavior, do not refactor. If CLI output is missing detector honesty status, add concise lines after processing that show detector mode and whether the run is simulated based on the first record. Keep baseline detector explicitly simulated and do not rename truth metadata fields because downstream dashboard and evaluator code already consume them.
  </action>
  <verify>
    <automated>python -m pytest tests/test_yolo_detector.py tests/test_model_readiness_eval.py tests/test_satellite_edge_bandwidth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_yolo_detector.py tests/test_model_readiness_eval.py tests/test_satellite_edge_bandwidth.py -q` passes.
    - `rg -n "detector_is_real|simulated|fallback_used|fallback_reason" satellite_edge_node tests scripts` shows fields preserved in detector, payload/telemetry tests, and readiness/evaluator surfaces.
    - No implementation claims baseline or fallback is real detection.
  </acceptance_criteria>
  <done>HON-01, HON-02, and VAL-03 are protected by tests and narrow runtime messaging/readiness behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI detector selection -> model loader | User-selected `--detector yolo` crosses into optional local model/package setup. |
| Detector output -> payload/telemetry | Detector truth metadata becomes judge-visible artifact state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | `scripts/check_model_ready.py` | mitigate | Readiness output must expose weights/package state and not report real detector availability unless both are present. |
| T-01-02-02 | Repudiation | `orbital_pass.main` | mitigate | Strict failure message and metadata fields make fallback/simulation state explicit in CLI output and artifacts. |
| T-01-02-03 | Information Disclosure | telemetry metadata | accept | Local demo telemetry intentionally exposes detector mode; no secrets are involved in Phase 1. |
</threat_model>

<verification>
Run `python -m pytest tests/test_yolo_detector.py tests/test_model_readiness_eval.py tests/test_satellite_edge_bandwidth.py -q`.
Run `python scripts/check_model_ready.py --json` and confirm it reports `ready_for_strict_yolo` truthfully for the local machine.
</verification>

<success_criteria>
HON-01, HON-02, and VAL-03 are satisfied when readiness is machine-readable, strict YOLO is loud by default on missing setup, and simulation/fallback metadata remains visible in all generated artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-strict-demo-chain/01-strict-demo-chain-02-SUMMARY.md`
</output>
