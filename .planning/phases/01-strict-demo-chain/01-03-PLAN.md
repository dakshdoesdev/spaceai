---
phase: 01-strict-demo-chain
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - satellite_edge_node/orbital_pass.py
  - satellite_edge_node/payloads.py
  - tests/test_satellite_edge_bandwidth.py
requirements:
  - DEMO-02
  - DEMO-03
  - DEMO-04
autonomous: true
must_haves:
  truths:
    - "User can process at least one readable raster fixture through the satellite edge node."
    - "User can inspect payload JSON containing detector metadata, triage action, byte accounting, and crop/full-downlink decision."
    - "Every non-null payload `crop_ref` points to a real non-empty crop file."
  artifacts:
    - path: "satellite_edge_node/payloads.py"
      provides: "Payload JSON and crop-reference integrity"
      contains: "crop_ref"
    - path: "tests/test_satellite_edge_bandwidth.py"
      provides: "Readable raster, payload, telemetry, and crop file proof"
      contains: "crop_ref"
  key_links:
    - from: "satellite_edge_node/payloads.py"
      to: "transmission_queue/crops"
      via: "generate_crop_file"
      pattern: "Image.open.*crop"
    - from: "payload JSON"
      to: "crop artifact"
      via: "crop_ref path"
      pattern: "Path\\(payload\\[\"crop_ref\"\\]\\)\\.exists"
---

<objective>
Verify real crop artifact generation and payload references for Phase 1.

Purpose: DEMO-02, DEMO-03, and DEMO-04 require a concrete proof chain from readable raster input through edge processing to payload JSON and an actual crop file.
Output: Tests and narrow payload fixes so payload JSON contains the required inspection fields and every crop claim is file-backed.
</objective>

<execution_context>
@/home/dux/Work/tries/SpaceAI/.codex/get-shit-done/workflows/execute-plan.md
@/home/dux/Work/tries/SpaceAI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-strict-demo-chain/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@satellite_edge_node/payloads.py
@satellite_edge_node/orbital_pass.py
@tests/test_satellite_edge_bandwidth.py
@.planning/phases/01-strict-demo-chain/01-strict-demo-chain-01-SUMMARY.md

<interfaces>
Created by Plan 01:
```python
simulate_orbital_pass(..., reset_queue=True) -> list[dict]
```
Use this for fresh queue behavior in crop proof tests.

From `satellite_edge_node/payloads.py`:
```python
def generate_crop_file(tile_path: Path, detection: DetectionResult, crops_dir: Path) -> CropArtifact: ...
def build_transmission_payload(detection: DetectionResult, tile_path: Path, crop_artifact: CropArtifact | None = None) -> dict[str, Any]: ...
def telemetry_record(...) -> dict[str, Any]: ...
```
Current payload alert fields include detector truth metadata and `crop_ref`; telemetry includes byte accounting and `action`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock payload JSON inspection fields and crop-ref existence</name>
  <files>tests/test_satellite_edge_bandwidth.py</files>
  <read_first>
    - tests/test_satellite_edge_bandwidth.py
    - satellite_edge_node/payloads.py
    - satellite_edge_node/orbital_pass.py
    - .planning/phases/01-strict-demo-chain/01-RESEARCH.md
  </read_first>
  <behavior>
    - A readable PNG fixture with sidecar bbox produces an alert payload JSON.
    - Alert payload JSON contains detector metadata, triage action, byte accounting fields, and crop/full-downlink decision fields.
    - For every payload with non-null `crop_ref`, the referenced path exists and has size greater than zero.
  </behavior>
  <action>
    Extend the existing crop test or add a focused test in `OrbitalPassTests` using Pillow to create a PNG fixture and sidecar metadata. Call `simulate_orbital_pass(raw_tiles, queue, detector_mode="baseline", reset_queue=True)` so it depends on Plan 01 freshness. Load the payload JSON and telemetry record. Assert payload JSON includes `detector_mode`, `detector_is_real`, `simulated`, `action`, `crop_ref`, `crop_error`, and byte accounting fields matching telemetry: `original_payload_bytes`, `json_payload_bytes`, `crop_payload_bytes`, `transmitted_payload_bytes`, and `bandwidth_saved_bytes`. Assert `Path(payload["crop_ref"]).exists()` and `Path(payload["crop_ref"]).stat().st_size > 0` whenever `crop_ref` is not null.
  </action>
  <verify>
    <automated>python -m pytest tests/test_satellite_edge_bandwidth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test fails if payload JSON lacks action or byte accounting fields.
    - Test fails if any non-null crop ref points to a missing or empty file.
    - Test uses a readable raster fixture, not an unreadable `.tile` blob, for the positive crop proof.
  </acceptance_criteria>
  <done>Automated coverage proves DEMO-02, DEMO-03, and DEMO-04 at the payload/crop artifact level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add required payload inspection fields without breaking telemetry</name>
  <files>satellite_edge_node/orbital_pass.py, satellite_edge_node/payloads.py, tests/test_satellite_edge_bandwidth.py</files>
  <read_first>
    - satellite_edge_node/orbital_pass.py
    - satellite_edge_node/payloads.py
    - tests/test_satellite_edge_bandwidth.py
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <behavior>
    - Alert payload JSON contains detector metadata, triage action, byte accounting, crop reference, and crop error.
    - Dropped payload JSON contains at least detector metadata and `action="DROP_RAW_TILE"`; it must not claim a crop.
    - Telemetry byte math remains the source of truth and stays consistent with payload fields.
  </behavior>
  <action>
    Update `build_transmission_payload()` and its call site in `simulate_orbital_pass()` to include the inspection fields required by DEMO-03 while preserving existing keys and truth metadata. Use this deterministic sequence in `simulate_orbital_pass()`: build the initial payload, add top-level `action`, add top-level `byte_accounting` with `original_payload_bytes`, `json_payload_bytes=0`, `crop_payload_bytes`, `transmitted_payload_bytes=0`, and `bandwidth_saved_bytes=0`, then encode and update the three size-derived fields in a short fixed-point loop until `len(encode_payload(payload))` stops changing or for at most 3 iterations. After stabilization, write the final payload bytes and pass the same final size fields into `telemetry_record()`. Keep `action` top-level in payload JSON and update tests to compare payload `byte_accounting` fields to telemetry fields.
  </action>
  <verify>
    <automated>python -m pytest tests/test_satellite_edge_bandwidth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_satellite_edge_bandwidth.py -q` passes.
    - `rg -n "original_payload_bytes|json_payload_bytes|crop_payload_bytes|transmitted_payload_bytes|bandwidth_saved_bytes|crop_ref" satellite_edge_node/payloads.py tests/test_satellite_edge_bandwidth.py` shows payload-side coverage.
    - Any non-null `crop_ref` in tested payload JSON exists and is non-empty.
  </acceptance_criteria>
  <done>Payload JSON is self-contained enough for judge inspection and crop references are backed by real files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw image file -> Pillow crop generation | Local input image bytes are parsed by Pillow to produce crop artifacts. |
| Payload JSON -> filesystem crop path | A payload-visible `crop_ref` points back to a local generated file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | `build_transmission_payload` | mitigate | Tests compare payload byte/crop fields against telemetry and file existence. |
| T-01-03-02 | Information Disclosure | `crop_ref` paths | accept | Phase 1 is a local demo; public redaction is handled in later public-doc/submission phases. |
| T-01-03-03 | Denial of Service | `generate_crop_file` | accept | Demo uses tiny local fixtures; no hosted image upload path exists. |
</threat_model>

<verification>
Run `python -m pytest tests/test_satellite_edge_bandwidth.py -q`.
If Plan 01 is complete, run a temp-path smoke command with `--reset-queue` and inspect one generated payload and crop file.
</verification>

<success_criteria>
DEMO-02, DEMO-03, and DEMO-04 are satisfied when a readable raster fixture produces an inspectable payload JSON and every claimed crop is an actual non-empty file.
</success_criteria>

<output>
After completion, create `.planning/phases/01-strict-demo-chain/01-strict-demo-chain-03-SUMMARY.md`
</output>
